plugins {
    id 'java-library'
    id 'org.gradle.java.experimental-jigsaw' version '0.1.1'  // <1>
}

dependencies {
    api project(':boot')
    api project(':a:a-api')
    runtimeOnly project(':a:a-impl')
    runtimeOnly project(':singleton')
}

javaModule.name = 'core'